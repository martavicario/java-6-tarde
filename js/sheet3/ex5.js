// Función normal
const numeros = [1, 2, 3];
const dobles = numeros.map(function(n) {
return n * 2;
});
// Función de flecha
const numerosFlecha = [1, 2, 3];
const doblesFlecha = numerosFlecha.map((n) => {
return n * 2;
});